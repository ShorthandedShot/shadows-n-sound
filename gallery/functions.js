function toggleSound() {
  var audioElem = document.getElementById('audio');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}

function toggleSoundOne() {
  var audioElem = document.getElementById('audio1');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}

function toggleSoundTwo() {
  var audioElem = document.getElementById('audio2');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}

function toggleSoundThree() {
  var audioElem = document.getElementById('audio3');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}

function toggleSoundFour() {
  var audioElem = document.getElementById('audio4');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
